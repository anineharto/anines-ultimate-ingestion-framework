[project]
name = "extract_load"
version = "0.1.0"
description = "Databricks Asset Bundle project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "databricks-sdk>=0.99.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
